import { StyleSheet } from "react-native";

export const s = StyleSheet.create({
  textProfile: { paddingVertical: 25, borderWidth: 2, borderColor: "gray", fontSize:20, backgroundColor:'peru', color:"white"  },
});
